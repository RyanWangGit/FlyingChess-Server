language: java
jdk:
  - oraclejdk8
sudo: 
  - required
before_install:
  - sudo apt-get update
install:
  - sudo apt-get install expect
script:
  - mvn assembly:assembly
after_success:
  - chmod +x ./scripts/deploy.sh
  - ./scripts/deploy.sh